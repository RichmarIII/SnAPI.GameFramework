# SnAPI::GameFramework::ComponentContractConcept

Compile-time concept describing the API surface expected of component-like types.

`ComponentContractConcept` captures the owner binding, activation, replication, identity, world access, RPC, and tick-phase methods expected by generic component-aware code. `BaseComponent` provides the canonical implementation.
