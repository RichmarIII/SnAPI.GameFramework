# SnAPI::GameFramework::THasAssetRefDefaultName

Trait that detects whether an asset-reference name tag exposes a static default asset name.

A tag participates when it provides a `Value` member containing the default name text.
